services:
  sscrt-provider:
    build: .
    image: sscrt-provider:local
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/data/provider.sqlite3
      - PORT=8787
    volumes:
      - ./data:/data
    ports:
      - "8787:8787"
